package com.subterra.recipe;
/*
import net.minecraft.recipe.AbstractCookingRecipe;
import net.minecraft.recipe.Recipe;
import net.minecraft.recipe.RecipeSerializer;
import net.minecraft.recipe.RecipeType;
import net.minecraft.registry.Registries;
import net.minecraft.registry.Registry;
import net.minecraft.util.Identifier;

public class ModRecipeTypes {
    public static final RecipeType<? extends SteelSmeltingRecipe> STEEL_SMELTING = register("steel_smelting");
    public static <T extends Recipe<?>> RecipeType<T> register(final String id) {
        return (RecipeType)Registry.register(Registries.RECIPE_TYPE, Identifier.of("subterra", id), new RecipeType<T>() {
            public String toString() {
                return id;
            }
        });
    }
}*/
